# it is just for taking input and throughing output , it also return the value 
#  syntax  function_name = lambda variable_name i.e x , y : operation i.e x + y 

add = lambda x , y : x*y

print (add(12,23))
